apiVersion: v1
kind: ConfigMap
metadata:
  name: nexus-config
  namespace: nexus
data:
  NODE_ENV: "production"
  LOG_LEVEL: "warn"
  TRACING_ENABLED: "true"
  METRICS_ENABLED: "true"
  RATE_LIMIT_ENABLED: "true"
  DB_POOL_MIN: "5"
  DB_POOL_MAX: "20"
  REDIS_MAX_RETRIES: "5"
