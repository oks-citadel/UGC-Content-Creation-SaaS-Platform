# =============================================================================
# Asset Service - Kubernetes Manifests
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: creatorbridge
  labels:
    app.kubernetes.io/part-of: creatorbridge
---
# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: asset-service
  namespace: creatorbridge
  labels:
    app: asset-service
    app.kubernetes.io/name: asset-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: creatorbridge
spec:
  replicas: 2
  selector:
    matchLabels:
      app: asset-service
  template:
    metadata:
      labels:
        app: asset-service
        app.kubernetes.io/name: asset-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8088"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: asset-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: asset-service
          image: creatorbridge.azurecr.io/asset-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8088
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "8088"
            - name: SERVICE_NAME
              value: "asset-service"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: asset-service-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: shared-secrets
                  key: redis-url
            - name: AZURE_STORAGE_CONNECTION_STRING
              valueFrom:
                secretKeyRef:
                  name: asset-service-secrets
                  key: azure-storage-connection-string
            - name: AZURE_STORAGE_CONTAINER_UPLOADS
              value: "uploads"
            - name: AZURE_STORAGE_CONTAINER_ASSETS
              value: "assets"
            - name: AZURE_CDN_HOSTNAME
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: cdn-hostname
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: asset-service
                topologyKey: kubernetes.io/hostname
---
# Service
apiVersion: v1
kind: Service
metadata:
  name: asset-service
  namespace: creatorbridge
  labels:
    app: asset-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: asset-service
---
# ServiceAccount
apiVersion: v1
kind: ServiceAccount
metadata:
  name: asset-service
  namespace: creatorbridge
  labels:
    app: asset-service
---
# HorizontalPodAutoscaler
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: asset-service
  namespace: creatorbridge
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: asset-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max
---
# PodDisruptionBudget
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: asset-service
  namespace: creatorbridge
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: asset-service
